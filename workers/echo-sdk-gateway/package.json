{
  "name": "echo-sdk-gateway",
  "version": "3.2.0",
  "description": "Echo SDK Gateway — Cloudflare Worker exposing Engine Runtime, Vault, Shared Brain, Knowledge Forge as REST API. GPT Action #7.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0",
    "wrangler": "^3.60.0"
  }
}
